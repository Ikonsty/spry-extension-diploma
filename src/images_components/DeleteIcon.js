import React from "react";
import Delete from '../images/delete.svg';

const DeleteIcon = () => (
    <img
        src={Delete}
        alt='Delete'
        style={{
            width: '20px',
            height: '20px',
        }}
    />
);

export default DeleteIcon;