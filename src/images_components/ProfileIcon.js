import React from "react";
import Profile from "../images/profile"

const ProfileIcon = () => (
    <img
        src={Profile}
        alt='Settings'
        style={{
            width: '20px',
            height: '20px',
        }}
    />
);

export default ProfileIcon;